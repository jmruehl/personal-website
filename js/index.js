const entry = document.querySelector('#most-recent-entry')

const timeStamps = document.querySelectorAll('time')


// console.log(timeStamps)
// console.log(entry)


// To Do: Get entry from logbook.html to be displayed in index.html. How do I connect these two files via js?

// entry.innerHTML = 'Dies ist mein innerHTML' <- The most recent entry needs to be displayed here.